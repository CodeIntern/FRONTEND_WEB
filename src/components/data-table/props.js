// External
import PropTypes from 'prop-types';

const propTypes = {
    /** Text of the link */
    text: PropTypes.string,

    /** Source of the link */
    src: PropTypes.string,

    /** Style for the link */
    style: PropTypes.string
};

const defaultProps = {
};

export default {
  propTypes,
  defaultProps,
};
